# 반응하는 우포포 — 단계별 실행 가이드

기획서(upopo_interactive_기획서.md)의 실행편 · 2026.08
원칙: 가장 불확실한 것부터 죽인다. 각 단계의 완료 기준을 통과하기 전에 다음 단계 금지.

---

## 사용 툴 총정리

| 구간 | 툴 | 비용 | 용도 |
|------|-----|------|------|
| 트래킹 | Python 3.10~3.11 + MediaPipe | 무료 | 웹캠에서 얼굴·손 위치 인식 |
| 신호 전달 | python-osc | 무료 | 인식 값을 TouchDesigner로 송신 |
| 실시간 비주얼 | TouchDesigner (비상업 무료판) | 무료 | 신호 수신, 상태 전환, 캐릭터 출력 |
| 캐릭터 제작 | Blender | 무료 | 우포포 모델·애니메이션 (idle/주목/손흔들기) |
| AI 보조 | Claude 등 생성형 AI | - | Python 코드 생성, TD 노드 길잡이 |
| 버전 관리 | Git + GitHub | 무료 | 커밋 기록 = 제작 과정 증거 |
| 촬영·편집 | 스마트폰 + 다빈치 리졸브 무료판 | 무료 | 데모 영상 |

바이브 코딩 배분: Python 구간 90% AI / TD 구간은 AI에게 "어떤 노드?"만 묻고 손은 직접 / 타이밍·이징 튜닝은 AI 0%.

---

## STEP 0 — 환경 세팅 (반나절)

할 일:
1. Python 3.10 또는 3.11 설치 → 가상환경 생성 (`python -m venv upopo`)
2. `pip install mediapipe opencv-python python-osc`
3. TouchDesigner 비상업 라이선스 가입·설치
4. GitHub 저장소 생성, 첫 커밋 (README 한 줄이라도)

완료 기준: 가상환경에서 `import mediapipe` 에러 없이 통과 + TD가 열림.

알면 좋을 지식:
- MediaPipe는 Python 최신 버전과 호환이 늦다 → 3.10/3.11 고정이 안전빵
- 가상환경을 쓰는 이유 = Rez가 하는 일의 1인용 버전(프로젝트별 패키지 격리). 면접에서 연결해 말할 수 있는 포인트

---

## STEP 1 — 기술 스파이크: 통로 뚫기

목표: 웹캠 → Python → OSC → TouchDesigner 신호가 흐르는 것만 확인. 비주얼 금지, 우포포 금지, 회색 박스면 충분.

할 일 순서:
1. MediaPipe 웹캠 예제 실행 — 화면에 내 손 랜드마크가 그려지는 것 확인
2. 코드에서 세 가지 값 추출: ①얼굴 크기(=거리 추정) ②얼굴 중심 x좌표 ③손 흔들기 여부(손목 x좌표의 좌우 왕복 감지)
3. python-osc로 세 값을 `/face/size` `/face/x` `/hand/wave` 주소로 초당 30회 송신
4. TD에서 OSC In CHOP 노드로 수신 → 값이 실시간으로 바뀌는 것 확인
5. 수신 값을 사각형(Rectangle TOP)의 크기·위치에 연결 — 내가 움직이면 박스가 따라 움직이면 통과

완료 기준: 내 움직임 → 박스 반응이 체감 지연 없이(0.1초 이내) 이어짐. 이 순간을 화면 녹화로 남길 것.

알면 좋을 지식:
- OSC = 미디어아트 업계의 표준 통신 규약. "UDP 위에서 /주소 값 형태로 쏘는 가벼운 신호"라고 이해하면 충분. MIDI의 후계자 격
- MediaPipe 좌표는 0~1 정규화 값. 픽셀이 아니라 비율이라 해상도가 달라도 코드가 안 깨짐
- 거리 센서 없이 거리를 아는 법 = 얼굴 바운딩박스가 커지면 가까운 것. 근사치지만 이 용도엔 충분
- TD 기초 어휘: CHOP(숫자·신호), TOP(이미지·픽셀), SOP(3D 지오메트리), COMP(묶음). 이 네 단어만 알면 튜토리얼이 읽힌다

---

## STEP 2 — 캐릭터 반입 판정 (플랜 A/B 결정)

목표: 우포포를 TD에 넣는 방식을 이틀 안에 확정한다.

할 일 순서:
1. Blender에서 우포포에 최소 애니메이션 3종 제작: idle(숨쉬기·잔움직임 루프) / 주목(카메라 쪽 전환) / 손흔들기. 각 3~5초 루프
2. 플랜 A 시도: FBX/glTF로 내보내 TD에 임포트, 본(bone) 제어 또는 애니메이션 클립 전환이 되는지 실험 — 제한시간 이틀
3. 이틀 안에 안 풀리면 즉시 플랜 B: Blender에서 상태별 애니메이션을 투명배경(RGBA) 영상 시퀀스로 렌더 → TD에서 Movie File In TOP으로 불러와 상태에 따라 클립 전환

판정 기준(자존심 배제, 기계적으로):
- A가 이틀 내 성공 → A로 진행
- 실패 → B로 전환. B는 후퇴가 아님: 관객은 차이를 모르고, Blender 렌더 품질(Cycles 라이팅, 퍼 등)을 그대로 쓸 수 있어 비주얼은 오히려 상승

완료 기준: TD 화면에서 우포포가 보이고, 키 입력(수동)으로 상태 전환이 되는 것.

알면 좋을 지식:
- TD의 3D 임포트는 게임엔진만큼 매끄럽지 않다. 실무에서도 "렌더 시퀀스 재생 전환"은 전시 콘텐츠의 정석 기법 중 하나 — 부끄러운 우회로가 아니라 업계 표준 선택지
- 플랜 B로 갈 경우 시퀀스는 프레임 수를 아끼자: 24fps × 4초 × 3종이면 288장. EXR 말고 PNG(RGBA)면 용량·호환 무난
- 상태 전환 시 뚝 끊기지 않게 하려면 각 클립의 시작/끝 포즈를 idle 포즈로 통일해두기 — Blender 단계에서 미리 설계하면 TD에서 크로스페이드가 쉬워짐

---

## STEP 3 — 반응 R1 하나를 끝까지

목표: "사람 접근 → 우포포가 알아차림"만 완성. 나머지 반응 착수 금지.

할 일 순서:
1. TD에서 상태 머신 구성: `/face/size` 값에 임계값(threshold) 설정 → 넘으면 idle→주목 전환, 내려가면 일정 시간 뒤 idle 복귀
2. 전환에 지연(delay)과 보간(easing/lag) 추가 — Lag CHOP, Filter CHOP 활용
3. 타이밍 튜닝: 알아차림까지 0.5~0.8초 뜸, 복귀 전 1~2초 두리번(여운)

완료 기준: 제3자가 화면 앞을 지나가게 하고 "어? 얘가 나 봤어?" 반응이 나오면 통과. 본인 테스트만으로 통과 판정 금지.

알면 좋을 지식:
- 생명감의 8할은 지연과 이징. 즉각 반응(0.0초)은 센서, 0.5~0.8초 반응은 생명체로 느껴진다 — 애니메이션 12원칙의 anticipation과 같은 원리. 포폴 "반응 설계" 문단에 쓸 핵심 문장
- 임계값 하나로 하면 경계에서 상태가 파르르 떨림(채터링) → 임계값을 진입용/이탈용 두 개로 벌려서 해결(히스테리시스). 이 단어를 알고 쓰면 면접에서 급이 달라짐
- 이 단계 소요 시간이 곧 "반응 1개의 실제 비용" — 이 데이터로 R2·R3 유지/축소를 결정

---

## STEP 4 — 나머지 반응 + 감각 튜닝

목표: R2(시선 따라가기), R3(손 흔들기 화답) 추가. 단 STEP 3이 오래 걸렸다면 R3 과감히 삭제(반응 2개로도 생명감은 성립).

할 일 순서:
1. R2: `/face/x` 값을 우포포의 시선/몸 방향에 부드럽게 매핑(Lag CHOP 필수 — 즉각 추적은 소름, 반 박자 늦은 추적이 귀여움)
2. R3: `/hand/wave` 트리거 → 손흔들기 클립 재생 → idle 복귀
3. 전체 흐름 통합 튜닝: 상태 간 우선순위(흔들기 중 접근하면? 등) 정리, 전환 크로스페이드
4. idle 디테일: 완전 정지 없애기 — 미세한 흔들림·눈 깜빡임이 "살아있음"의 기본값

완료 기준: 제3자 테스트에서 웃음 또는 "오~" 소리가 나오는 것. 진지하게, 이게 기준.

알면 좋을 지식:
- 상태 우선순위는 표로 먼저 그리고 노드를 만들 것(예: wave > 주목 > idle). 즉흥으로 연결하면 스파게티 됨
- 전체 시간의 30%를 이 단계 튜닝에 배정 — 여기가 포폴 차별화 지점이자 AI가 못 해주는 "본인 지분"

---

## STEP 5 — 기록과 포장

목표: 산출물 3종 완성 → 즉시 지원 발송.

할 일 순서:
1. 촬영: 본인 아닌 타인을 세워 체험 장면 촬영(뒷모습+화면이 한 앵글에). 처음 보는 사람의 진짜 리액션이 핵심
2. 편집: 45초~1분 (idle 훅 5초 → 체험 30초 → 여운 10초). 세로 15초 릴스 컷 별도
3. 포폴 페이지 1장: 영상 / 파이프라인 다이어그램 / "반응 설계" 문단(지연·이징·히스테리시스 이야기)
4. GitHub 정리: README에 다이어그램 + 실행법 + 바이브 코딩 워크플로우 명시
5. 아이엠파인·커즈 소개 메일 발송 ← 프로젝트의 진짜 완료 조건

완료 기준: 지원 이력 0회 → 1회.

---

## 전체 공통 규칙

1. 각 STEP 종료 시 그 시점 결과물 짧게 녹화(회색 박스 시절부터) — 과정 콘텐츠 재료
2. 막힌 지 2시간 넘으면 AI에게 화면 상황을 그대로 설명하고 물을 것. 4시간 넘으면 그날은 우회(플랜 B적 사고)
3. 새 아이디어(음성 반응, 멀티유저, 이펙트)는 백로그.md에 적고 잊기
4. 포아 2R 일정과 충돌 시 무조건 2R 우선 — 이 문서는 어디까지나 병렬 트랙
